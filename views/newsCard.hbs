<div class="row">
    <div class="col s12 m6 offset-m3">
        <div class="card">
            <div class="card-image">
                <img alt="{{newsCard.title}}" src="{{newsCard.img}}">
            </div>
            <div class="card-content">
                <span class="card-title">{{newsCard.title}}</span>
                <p>{{newsCard.description}}</p>
            </div>
        </div>
    </div>
</div>